/bin/
/dist/
web/dist/
web/node_modules/
web/*.tsbuildinfo
node_modules/
*.log
.DS_Store

# Playwright (WP-1.5)
web/test-results/
web/playwright-report/
web/blob-report/
web/.playwright/

# MEMANTO project-local memory cache (auto-synced at session start, not a
# repo artifact). See CLAUDE.md.
MEMORY.md

# Agent git worktrees. Checkouts of branches that already live in the repo —
# the rest of .claude/ (agents, commands, skills) is tracked on purpose.
.claude/worktrees/
