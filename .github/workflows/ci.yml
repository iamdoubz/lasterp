name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
      - uses: actions/setup-node@v6
        with:
          node-version: "26"
      - run: npm install -g pnpm@9.15.0
      - run: pnpm install
      - uses: golangci/golangci-lint-action@v9
        with:
          version: latest
      - run: pnpm --dir web run lint
      - run: ./scripts/spdx-lint.sh
      - run: ./scripts/dco-check.sh origin/${{ github.base_ref || 'main' }} HEAD
        if: github.event_name == 'pull_request'

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
      - uses: actions/setup-node@v6
        with:
          node-version: "26"
      - run: npm install -g pnpm@9.15.0
      - run: pnpm install
      - run: go test ./...
      - run: pnpm --dir web run test

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
      - name: build solo binary
        run: go build -o bin/lasterp ./cmd/lasterp
      - name: build docker image
        run: docker build -f deploy/Dockerfile -t lasterp:ci .
      - uses: azure/setup-helm@v5
      - name: lint helm chart
        run: helm lint deploy/helm/lasterp
